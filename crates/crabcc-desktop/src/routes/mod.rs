//! Top-level route views. One module per dashboard route.
//! `dashboard` is the home / overview; `graph` is the relations-graph
//! viewer. A.6 splits out `logs`, `system`, and `knowledge`.

pub mod dashboard;
pub mod graph;
