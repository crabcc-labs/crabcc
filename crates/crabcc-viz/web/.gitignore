node_modules/
dist/main.js
dist/main.js.map
dist/main.css
dist/main.css.map
# Tailwind v4 generated bundle (track B.1) — produced by
# `bun run tailwind:build` from src/tailwind.input.css. Imported by
# src/main.tsx; ignored here so the generated artefact doesn't
# pollute git history.
src/tailwind.generated.css
# Keep dist/live.html committed — the Rust crate include_str!s it at
# build time so the workspace builds without bun. Regenerate via
# `bun run build` from this directory after editing src/.
