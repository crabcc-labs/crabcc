{
  "mcpServers": {
    "crabcc": {
      "command": "crabcc",
      "args": ["--mcp", "--dev"],
      "description": "sym · refs · callers · outline · files · fuzzy for symbol lookup. --dev surface adds _openapi + _health diagnostics. Use --count or --files-only flags whenever the full hit list is not needed (saves 99%+ tokens). Skip memory.* unless explicitly working on the memory layer."
    },

    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "description": "query-docs for Rust crates in this repo: tokio · serde · rusqlite · tantivy · clap · anyhow · axum. resolve-library-id optional — skip if library ID already known (format /org/project). Default: keep query-docs responses to 5000 tokens; fall back to a narrower query if the first result is too broad."
    }
  }
}
