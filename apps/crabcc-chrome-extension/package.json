{
  "name": "@crabcc/chrome-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Phase-0 MV3 extension that drives window.__crabcc__ on the active tab.",
  "type": "module",
  "scripts": {
    "build": "bun run build.mjs",
    "watch": "bun run build.mjs --watch",
    "typecheck": "bun x tsc --noEmit -p ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0"
  }
}
